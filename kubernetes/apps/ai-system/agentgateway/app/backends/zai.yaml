---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: zai-secret
  namespace: ai-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: zai-secret
    template:
      data:
        Authorization: "{{ .ZAI_API_KEY }}"
  dataFrom:
    - extract:
        key: ai-keys
---
# Z.AI AgentgatewayBackend (OpenAI-compatible)
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: zai
  namespace: ai-system
spec:
  ai:
    provider:
      openai: {}
      host: api.z.ai
      port: 443
  policies:
    tls: {}
    auth:
      secretRef:
        name: zai-secret
    ai:
      routes:
        "/v1/chat/completions": Completions
        "/v1/embeddings": Passthrough
        "/v1/models": Passthrough
        "*": Passthrough
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: zai
  namespace: ai-system
spec:
  parentRefs:
    - name: internal
    - name: internal-noauth
    - name: public
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /zai
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api/paas/v4
      backendRefs:
        - name: zai
          group: agentgateway.dev
          kind: AgentgatewayBackend
