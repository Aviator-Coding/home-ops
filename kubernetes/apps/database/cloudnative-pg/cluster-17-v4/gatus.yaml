---
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-17-v4-gatus-ep
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: postgres-17-v4
        group: database
        url: tcp://postgres-17-v4-rw.database.svc.cluster.local:5432
        interval: 1m
        ui:
          hide-url: true
          hide-hostname: true
        conditions:
          - "[CONNECTED] == true"
        alerts:
          - type: pushover
