---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
  - ./externalsecret.yaml
  - ./dashboards/ceph
  - ./dashboards/litellm
  # ./dashboard is an old sidecar home.json with a stale datasource uid.
  # The landing dashboard is dashboards.default.home-dashboard in the HelmRelease.
  # - ./dashboard
