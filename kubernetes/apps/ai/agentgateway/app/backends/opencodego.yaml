---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opencodego-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: opencodego-secret
    template:
      data:
        Authorization: "{{ .OPENCODE_GO_API_KEY }}"
  dataFrom:
    - extract:
        key: ai-keys
---
# OpenCode Go AgentgatewayBackend (OpenAI-compatible)
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: opencodego
spec:
  ai:
    provider:
      openai: {}
      host: opencode.ai
      port: 443
  policies:
    tls: {}
    auth:
      secretRef:
        name: opencodego-secret
    ai:
      routes:
        "/v1/chat/completions": Completions
        "/v1/embeddings": Passthrough
        "/v1/models": Passthrough
        "*": Passthrough
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: opencodego
spec:
  parentRefs:
    - name: internal
    - name: internal-noauth
    - name: public
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /opencodego
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /zen/go
      backendRefs:
        - name: opencodego
          group: agentgateway.dev
          kind: AgentgatewayBackend
