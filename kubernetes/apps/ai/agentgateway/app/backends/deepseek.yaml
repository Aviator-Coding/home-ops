---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: deepseek-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: deepseek-secret
    template:
      data:
        Authorization: "{{ .DEEPSEEK_API_KEY }}"
  dataFrom:
    - extract:
        key: ai-keys
---
# deepseek AgentgatewayBackend (OpenAI-compatible). Reached ONLY via the
# unified /v1 router (../httproute-unified.yaml) by model name
# (deepseek-chat/reasoner — bare deepseek-v4-* is OpenCode Go, deepseek/...
# slugs are OpenRouter). No per-provider path route exists anymore.
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: deepseek
spec:
  ai:
    provider:
      openai: {}
      host: api.deepseek.com
      port: 443
  policies:
    tls: {}
    auth:
      secretRef:
        name: deepseek-secret
    ai:
      routes:
        "/v1/chat/completions": Completions
        "/v1/embeddings": Passthrough
        "/v1/models": Passthrough
        "*": Passthrough
