---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-temp
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  storageClassName: ceph-filesystem
