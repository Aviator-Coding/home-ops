---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app oauth2-proxy
spec:
  driftDetection:
    mode: enabled
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    # https://github.com/bjw-s-labs/helm-charts/blob/main/charts/library/common/values.yaml
    controllers:
      oauth2-proxy:
        strategy: RollingUpdate
        replicas: 1
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: quay.io/oauth2-proxy/oauth2-proxy
              tag: v7.15.1@sha256:39f08531587045a5443db43299bf8495696c99ed8d0e1fdcbe60534f7c02ce14
            envFrom:
              - secretRef:
                  name: oauth2-proxy-env
            env:
              OAUTH2_PROXY_PROVIDER: github
              OAUTH2_PROXY_COOKIE_DOMAINS: ".${SECRET_DOMAIN}"
              OAUTH2_PROXY_COOKIE_SECURE: "true"
              OAUTH2_PROXY_COOKIE_HTTPONLY: "true"
              OAUTH2_PROXY_COOKIE_SAMESITE: "lax"
              OAUTH2_PROXY_COOKIE_EXPIRE: "168h0m0s"
              OAUTH2_PROXY_COOKIE_REFRESH: "60m0m0s"
              OAUTH2_PROXY_EMAIL_DOMAINS: "*"
              OAUTH2_PROXY_SCOPE: openid profile email
              OAUTH2_PROXY_HTTP_ADDRESS: 0.0.0.0:4180
              OAUTH2_PROXY_METRICS_ADDRESS: 0.0.0.0:44180
              OAUTH2_PROXY_PASS_AUTHORIZATION_HEADER: "true"
              OAUTH2_PROXY_PASS_HOST_HEADER: "true"
              OAUTH2_PROXY_PASS_ACCESS_TOKEN: "true"
              OAUTH2_PROXY_PASS_USER_HEADERS: "true"
              OAUTH2_PROXY_PREFER_EMAIL_TO_USER: "true"
              OAUTH2_PROXY_SET_AUTHORIZATION_HEADER: "true"
              OAUTH2_PROXY_SET_XAUTHREQUEST: "true"
              OAUTH2_PROXY_REVERSE_PROXY: "true"
              OAUTH2_PROXY_SILENCE_PING_LOGGING: "true"

              OAUTH2_PROXY_SESSION_STORE_TYPE: redis
              OAUTH2_PROXY_REDIS_CONNECTION_URL: redis://oauth2-proxy-dragonfly:6379
              OAUTH2_PROXY_WHITELIST_DOMAINS: "*.${SECRET_DOMAIN}"
              OAUTH2_PROXY_REDIRECT_URL: "https://oauth.${SECRET_DOMAIN}/oauth2/callback"
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /ping
                    port: 4180
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet:
                    path: /ready
                    port: 4180
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 1
                  failureThreshold: 3
              startup:
                enabled: true
                spec:
                  failureThreshold: 30
                  periodSeconds: 10

            resources:
              requests:
                cpu: 100m
              limits:
                memory: 1Gi

    service:
      app:
        controller: *app
        ports:
          http:
            port: &port 4180
          metrics:
            port: 44180

    serviceMonitor:
      app:
        serviceName: *app
        endpoints:
          - port: metrics
            scheme: http
            interval: 30s
            scrapeTimeout: 5s

    route:
      main:
        annotations:
          external-dns/unifi: "true"
          external-dns/cloudflare: "true"
          external-dns.alpha.kubernetes.io/target: "external.${SECRET_DOMAIN}"
          gatus.home-operations.com/endpoint: |
            group: external
            client:
              dns-resolver: tcp://1.1.1.1:53
            conditions:
              - "[STATUS] == 404"
        labels:
          tarampampam/error-pages: disabled
        hostnames:
          - oauth.${SECRET_DOMAIN}
        parentRefs:
          - name: envoy-external
            namespace: network
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
